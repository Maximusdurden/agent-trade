# Alpaca Paper Trading Credentials
# Get these from your Alpaca dashboard (make sure you are on the "Paper Trading" view)
ALPACA_API_KEY=your_alpaca_api_key_here
ALPACA_SECRET_KEY=your_alpaca_secret_key_here
ALPACA_PAPER=True

# LLM Configurations
# Supported providers: 'gemini', 'openrouter'
# NOTE: 'openrouter' is recommended (avoids direct Google Cloud billing for
# Gemini API). Keeps you on the cheaper google/gemini-2.5-flash daily driver.
LLM_PROVIDER=openrouter

# Gemini Configuration
# Get API key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Supported models: gemini-3.5-flash (default), gemini-3.5-pro, gemini-2.5-flash (legacy)
GEMINI_MODEL=gemini-3.5-flash

# OpenRouter Configuration (alternative to Gemini)
# Get API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Configurations (Use when LLM_PROVIDER=openrouter)
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
MODEL_HEAVYWEIGHT=deepseek/deepseek-r1
MODEL_DAILY_DRIVER=qwen/qwen-2.5-coder-32b-instruct
MODEL_UTILITY=openrouter/free
ACTIVE_MODEL_TIER=daily_driver
BRAIN_MODEL_TIER=daily_driver
# Max output tokens for the brain's per-ticker decision JSON. The brain emits one
# verbose decision per appraised ticker; keep this well above the expected size
# so the JSON is not truncated mid-response (which forces a rule-based fallback).
BRAIN_MAX_OUTPUT_TOKENS=8192

# Jira Connection Details (For automatic JIRA issue logging and logging overrides)
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your_jira_email_here
JIRA_API_TOKEN=your_jira_api_token_here
JIRA_PROJECT_KEY=TMCL

# GCS Configuration
# Path to service account key file (JSON)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json
# GCS bucket name
GCS_BUCKET_NAME=your-gcs-bucket-name

# Secure Dashboard Authentication Settings
# Set a strong password to lock down the dashboard. Leave empty to disable authentication.
DASHBOARD_PASSWORD=your_highly_secure_dashboard_password_here
SESSION_SALT=choose_some_random_salt_string_here

# Options Trading Configuration
# Kill-switch: set to true to enable options trading (long calls/puts only).
OPTIONS_ENABLED=false
# Default DTE window for option selection (days-to-expiry).
OPTIONS_DTE_MIN=30
OPTIONS_DTE_MAX=60
# Hard safety bounds on DTE (agent may override within these only).
OPTIONS_DTE_HARD_MIN=14
OPTIONS_DTE_HARD_MAX=90
# Max % of equity per option position (cost = ask * 100 * contracts).
OPTIONS_MAX_ALLOCATION_PCT=0.05
# Max option contracts per ticker.
OPTIONS_MAX_CONTRACTS_PER_TICKER=5
# Conviction threshold (0.0-1.0): >= threshold routes to option path (leverage).
OPTIONS_CONVICTION_THRESHOLD=0.7
# Auto-close option positions when DTE <= this value (avoid exercise/assignment).
OPTIONS_AUTO_CLOSE_DTE=3
# OTM% window for strike selection.
OPTIONS_OTM_PERCENT_MIN=0.01
OPTIONS_OTM_PERCENT_MAX=0.10
# Options risk controls (event gate + greeks exposure caps + optional EOD-flat).
OPTIONS_EVENT_GATE_ENABLED=true
OPTIONS_EVENT_GATE_INCLUDE_FOMC=true
OPTIONS_VEGA_CAP_MV_PCT=0.02
OPTIONS_DELTA_CAP_PCT=0.15
OPTIONS_EOD_FLAT=false

# Correlation / Concentration Guardrail
# Max total exposure across a correlated cluster (e.g. all crypto, tech mega-caps)
# as a % of equity. Prevents an oversized, undiversified single-theme bet.
MAX_CLUSTER_ALLOCATION_PCT=0.40

# Per-Ticker Loss / Whipsaw Circuit Breaker
# Block BUYs to a symbol that keeps losing (consecutive losses) or whipsaws.
MAX_CONSECUTIVE_LOSSES=3
MAX_WHIPSAW_RATIO=0.60
MIN_WHIPSAW_TRADES=4
CIRCUIT_BREAKER_LOOKBACK_DAYS=90

# Crypto TP/SL Bracket Support
# Attach bracket take-profit/stop-loss to crypto BUYs (24/7 protection).
CRYPTO_BRACKET_ENABLED=true
CRYPTO_TAKE_PROFIT_PCT=0.05
CRYPTO_STOP_LOSS_PCT=0.03

# Volatility-Based Position Sizing
# Scale down BUY size for high-volatility assets (ATR% above baseline).
VOL_SIZING_ENABLED=true
VOL_SIZING_BASELINE_ATR_PCT=2.0
VOL_SIZING_MIN_ALLOCATION_PCT=0.02

# Intra-Day PnL Circuit Breaker
# Block new BUYs when the day's realized + unrealized loss exceeds this % of equity.
INTRADAY_LOSS_LIMIT_PCT=0.04
INTRADAY_BREAKER_ENABLED=true


